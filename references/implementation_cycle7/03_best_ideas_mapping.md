# Best Ideas Mapping - Cycle 7 (2026-02-25)

1. Preset operating modes
- Implemented: anomaly threshold profiles (`strict`, `default`, `lenient`).

2. Deterministic precedence
- Implemented: profile baseline + explicit query overrides.

3. Observability of configuration
- Implemented: anomalies response returns `profile` and `thresholdOverridesApplied`.
